apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: expense-tracker
spec:
  type: NodePort  # Use NodePort for development
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080  # Fixed NodePort for easy access
    protocol: TCP
    name: http
